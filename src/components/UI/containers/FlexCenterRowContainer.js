import React from "react";
import "./FlexCenterRowContainer.css";

const FlexCenterRowContainer = props => {
    return <div className="flex-center-row__container">{props.children}</div>
}

export default FlexCenterRowContainer;