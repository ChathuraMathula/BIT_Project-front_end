import React from "react";
import "./FlexCenterColumnContainer.css";

const FlexCenterColumnContainer = props => {
    return <div className="flex-center-column__container">{props.children}</div>
}

export default FlexCenterColumnContainer;